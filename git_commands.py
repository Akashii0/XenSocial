"""
simple Git commands:

git init           Initializes git
git add --all      Adds all files in directory into git
git commit -m      "-m' This is the commit message
git branch -M main    adds a branch
git remote add origin https://github.com/username/repo_name.git    connects to your repo
git push -u origin main

git checkout

in case of initial commit already created, we have to rebase
git pull --rebase origin name_of_branch
after the above command, you can now push.

git log


git config --global user.email "you@example.com"  This is to login if you haven't before
git config --global user.name "Your name"
"""